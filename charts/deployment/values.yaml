# Auto-generated values.yaml for deployment chart
# This file is generated by generate-values.sh script

# Global configuration for all applications (used as defaults)
global:
  project: nexus
  repoURL: https://github.com/thisisamank/nexus-gitops
  targetRevision: HEAD
  namespace: default
  server: https://kubernetes.default.svc

# Applications configuration
# Each application is auto-discovered from the deployment directory
applications:
  - name: app-1
    project: nexus
    repoURL: https://github.com/thisisamank/nexus-gitops
    targetRevision: HEAD
    path: charts/app
    valueFile: ../../apps/app-1/values.yaml
    namespace: default
    server: https://kubernetes.default.svc
    # Additional application-specific overrides can be added here
    # syncPolicy:
    #   automated:
    #     prune: true
    #     selfHeal: true

